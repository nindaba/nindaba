export interface Skills{
    back: Array<{
        name:string;
        image:string;
    }>
    front: Array<{
        name:string;
        image:string;
    }>
    other: Array<{
        name:string;
        image:string;
    }>
}